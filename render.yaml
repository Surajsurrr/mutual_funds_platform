# Render blueprint: one free web service (SPA + both APIs + workers) + free Postgres.
# Deploy: Render dashboard → New → Blueprint → pick this repo → Apply.
databases:
  - name: fundflow-db
    plan: free
    databaseName: fundflow
    user: fundflow

services:
  - type: web
    name: fundflow
    runtime: node
    plan: free
    # --include=dev: NODE_ENV=production is set at build time too, which would
    # otherwise make npm skip vite/tailwind and break the frontend build (127)
    buildCommand: npm ci --include=dev && npm run build && cd backend && npm ci --omit=dev
    startCommand: cd backend && node scripts/deploy-start.js
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 22.19.0
      - key: NODE_ENV
        value: production
      - key: API_ROLE
        value: web
      - key: DATABASE_URL
        fromDatabase:
          name: fundflow-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
